URL Shortner Service
-usingNode.js, Express and MongoDB.
-Routes
   POST /URL -Generates a new short URL and returns the shortened
              URL in the format  example.com/random-id
    GET/:id  -Redirects the user to original URL
    GET/URL/analytics/:id -Returns  the clicks for the provided short id

    --export -- router
-Controllers
    Functionality -- exports
-Models
    schema


for session

created middleware object user id